<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">


    <changeSet id="1" author="evgeniy-fedorchenko">

        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="users"/>
            </not>
        </preConditions>

        <comment>Create table 'users' for entity 'UserEntity'</comment>

        <createTable tableName="users">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="email" type="varchar(32)">
                <constraints nullable="false" unique="true" checkConstraint="char_length(email) >= 4"/>
            </column>
            <column name="first_name" type="varchar(16)">
                <constraints nullable="false" checkConstraint="char_length(first_name) >= 2"/>
            </column>
            <column name="last_name" type="varchar(16)">
                <constraints nullable="false" checkConstraint="char_length(last_name) >= 2"/>
            </column>
            <column name="phone" type="varchar(17)">
                <constraints nullable="false" checkConstraint="check (phone LIKE '(\+79|79|89)\d{9}')"/>
            </column>
            <column name="role" type="varchar(20)">
                <constraints nullable="true" checkConstraint="role IN ('ADMIN', 'USER')"/>
            </column>

            <!-- по разным данным пароль зашифрованный BCryptPasswordEncoder имеет длину от 60 до 62 символов -->
            <column name="password" type="varchar(62)">
                <constraints nullable="false" checkConstraint="char_length(password) >= 60"/>
            </column>

            <column name="image" type="oid">
                <constraints nullable="true"/>
            </column>
        </createTable>

    </changeSet>

</databaseChangeLog>