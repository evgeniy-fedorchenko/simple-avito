<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">


    <changeSet id="1" author="evgeniy-fedorchenko">

        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="users"/>
            </not>
        </preConditions>

        <comment>Create table 'users' for entity 'UserEntity'</comment>

        <createTable tableName="users">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="email" type="varchar(32)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="first_name" type="varchar(16)">
                <constraints nullable="false"/>
            </column>
            <column name="last_name" type="varchar(16)">
                <constraints nullable="false"/>
            </column>
            <column name="phone" type="varchar(17)">
                <constraints nullable="false"/>
            </column>
            <column name="role" type="varchar(20)">
                <constraints nullable="false"/>
            </column>

            <!-- по разным данным пароль зашифрованный BCryptPasswordEncoder имеет длину от 60 до 62 символов -->
            <column name="password" type="varchar(62)">
                <constraints nullable="false"/>
            </column>

            <column name="image" type="oid">
                <constraints nullable="true"/>
            </column>
        </createTable>

        <sql>
            ALTER TABLE users ADD CONSTRAINT email_pattern CHECK (email ~ '^[A-Za-z0-9._%-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$');
            ALTER TABLE users ADD CONSTRAINT email_min_len CHECK (length(email) >= 4);
            ALTER TABLE users ADD CONSTRAINT first_name_min_len CHECK (length(first_name) >= 2);
            ALTER TABLE users ADD CONSTRAINT last_name_min_len CHECK (length(last_name) >= 2);
            ALTER TABLE users ADD CONSTRAINT phone_pattern CHECK (phone ~ '^\+7\s?\(?\d{3}\)?\s?\d{3}-?\d{2}-?\d{2}$');
            ALTER TABLE users ADD CONSTRAINT role_values CHECK (role IN ('ADMIN', 'USER'));
            ALTER TABLE users ADD CONSTRAINT enc_pass_min_len CHECK (length(password) >= 60);
        </sql>

    </changeSet>

</databaseChangeLog>